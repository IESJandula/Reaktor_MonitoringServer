spring:
  jpa.hibernate.ddl-auto: create
  datasource:
    url: jdbc:mariadb://localhost:3306/reaktor
    username: reaktor
    password: wH92u61x~
    driver-class-name: org.mariadb.jdbc.Driver
server:
  port: 8084
springdoc.api-docs.path: /api-docs
logging:
    level:
        root: DEBUG
    file:
        name: /tmp/archivo.log
        max-size: 1MB
        max-history: 10
        total-size-cap: 10MB
reaktor:
  cronComputerOn: 5_000
  cronComputerOnDifference: 7_000
